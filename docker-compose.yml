services:
  # ----------------------------------------------------------------------------
  # MySQL Database Service
  # ----------------------------------------------------------------------------
  mysql:
    image: mysql:8.0
    container_name: shein-mysql
    restart: unless-stopped
    command: --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD must be set}
      MYSQL_DATABASE: ${DB_NAME:-shein}
    volumes:
      - shein_mysql_data:/var/lib/mysql
      - ./shein_database.sql:/docker-entrypoint-initdb.d/init.sql:ro
    ports:
      - "127.0.0.1:${DB_HOST_PORT:-3307}:3306"
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -p$${MYSQL_ROOT_PASSWORD} || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - shein-network

  # ----------------------------------------------------------------------------
  # Python Scraper API Service (Playwright Headless)
  # ----------------------------------------------------------------------------
  scraper:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: shein-scraper
    restart: unless-stopped
    environment:
      DB_URL: mysql+pymysql://root:${DB_PASSWORD:?DB_PASSWORD must be set}@mysql:3306/${DB_NAME:-shein}
      APP_SECRET: ${APP_SECRET:?APP_SECRET must be set}
      INTERNAL_API_TOKEN: ${INTERNAL_API_TOKEN:?INTERNAL_API_TOKEN must be set}
      SHEIN_LOCAL_API_TOKEN: ${SHEIN_LOCAL_API_TOKEN:?SHEIN_LOCAL_API_TOKEN must be set}
      PLAYWRIGHT_HEADLESS: "1"
      SHEIN_BASE_URL: ${SHEIN_BASE_URL:-https://ar.shein.com}
      SHEIN_PLAYWRIGHT_CHANNEL: chromium
      SHEIN_PLAYWRIGHT_PROFILES_DIR: /app/profiles
      SHEIN_MANUAL_LOGIN_TIMEOUT_SECONDS: ${SHEIN_MANUAL_LOGIN_TIMEOUT_SECONDS:-1800}
      SHEIN_ENABLE_REMOTE_BROWSER: ${SHEIN_ENABLE_REMOTE_BROWSER:-1}
      SHEIN_VNC_PASSWORD: ${SHEIN_VNC_PASSWORD:?SHEIN_VNC_PASSWORD must be set}
      DISPLAY: :99
    volumes:
      - shein_profiles:/app/profiles
    ports:
      - "127.0.0.1:${BROWSER_PORT:-6080}:6080"
    healthcheck:
      test: ["CMD-SHELL", "curl -f -s http://127.0.0.1:8000/ping || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - shein-network

  # ----------------------------------------------------------------------------
  # Fullstack Node.js Backend & React Application Service
  # ----------------------------------------------------------------------------
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: shein-app
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 8081
      HOST: "0.0.0.0"
      DB_HOST: mysql
      DB_PORT: 3306
      DB_NAME: ${DB_NAME:-shein}
      DB_USER: root
      DB_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD must be set}
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET must be set}
      CREDENTIAL_ENCRYPTION_KEY: ${CREDENTIAL_ENCRYPTION_KEY:?CREDENTIAL_ENCRYPTION_KEY must be set}
      SHEIN_LOCAL_API_BASE_URL: http://scraper:8000
      SHEIN_LOCAL_API_TOKEN: ${SHEIN_LOCAL_API_TOKEN:?SHEIN_LOCAL_API_TOKEN must be set}
      SHEIN_PROFILE_API_TIMEOUT_MS: ${SHEIN_PROFILE_API_TIMEOUT_MS:-10000}
      SHEIN_REMOTE_BROWSER_URL: ${SHEIN_REMOTE_BROWSER_URL:-https://shein-tracker.duckdns.org/vnc/vnc.html?path=websockify&autoconnect=true&resize=scale}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:?ALLOWED_ORIGINS must be set}
      SEED_ADMIN: "1"
      SEED_ADMIN_PASSWORD: ${SEED_ADMIN_PASSWORD:?SEED_ADMIN_PASSWORD must be set}
      SEED_DEMO_ACCOUNTS: "0"
    depends_on:
      mysql:
        condition: service_healthy
      scraper:
        condition: service_healthy
    ports:
      - "127.0.0.1:8081:8081"
    networks:
      - shein-network

  # ----------------------------------------------------------------------------
  # Nginx Gateway & Reverse Proxy (HTTP & HTTPS)
  # ----------------------------------------------------------------------------
  nginx:
    image: nginx:alpine
    container_name: shein-nginx
    restart: unless-stopped
    ports:
      - "127.0.0.1:${HTTP_HOST_PORT:-8088}:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
    networks:
      - shein-network

volumes:
  shein_mysql_data:
    driver: local
  shein_profiles:
    driver: local

networks:
  shein-network:
    driver: bridge
