import { useEffect, useState } from "react";
import { getMonths } from "../api/monthApi";
import { addLoss, getLossOrderCustomers, listLosses, reverseLoss, searchLossOrders, updateLoss } from "../api/lossesApi";
import "../operations.css";

const money = (value) => Number(value || 0).toFixed(2);

export default function LossesWorkspace() {
  const [months, setMonths] = useState([]);
  const [monthId, setMonthId] = useState("");
  const [orderQuery, setOrderQuery] = useState("");
  const [orders, setOrders] = useState([]);
  const [order, setOrder] = useState(null);
  const [customers, setCustomers] = useState([]);
  const [customerId, setCustomerId] = useState("");
  const [lossType, setLossType] = useState("out of stock item");
  const [amount, setAmount] = useState("");
  const [description, setDescription] = useState("");
  const [query, setQuery] = useState("");
  const [rows, setRows] = useState([]);
  const [status, setStatus] = useState("active");
  const [error, setError] = useState("");
  const [notice, setNotice] = useState("");

  useEffect(() => { getMonths().then((response) => { const list = Array.isArray(response) ? response : []; setMonths(list); if (list.length) setMonthId(String(list[0].id)); }).catch((err) => setError(err.message || "Failed to load months.")); }, []);
  useEffect(() => { if (!monthId) return; const timer = setTimeout(() => searchLossOrders(monthId, orderQuery).then((response) => setOrders(response?.orders || [])).catch((err) => setError(err.message || "Failed to search orders.")), 180); return () => clearTimeout(timer); }, [monthId, orderQuery]);
  useEffect(() => { if (!monthId) return; listLosses(monthId, query, status).then((response) => setRows(response?.losses || [])).catch((err) => setError(err.message || "Failed to load losses.")); }, [monthId, query, status]);

  const chooseOrder = async (nextOrder) => { setOrder(nextOrder); setCustomerId(""); try { const response = await getLossOrderCustomers(nextOrder.order_id); setCustomers(response?.customers || []); } catch (err) { setError(err.message || "Failed to load order customers."); } };
  const save = async (event) => { event.preventDefault(); const numericAmount = Number(amount); if (!order || !customerId || !lossType.trim() || !Number.isFinite(numericAmount) || numericAmount <= 0) { setError("Select an order and customer, then enter a positive loss amount."); return; } try { await addLoss({ month_id: Number(monthId), order_id: Number(order.order_id), customer_id: Number(customerId), loss_type: lossType, amount: numericAmount, description }); setAmount(""); setDescription(""); setNotice("Loss saved against the selected order and customer."); const response = await listLosses(monthId, query, status); setRows(response?.losses || []); } catch (err) { setError(err.message || "Failed to save loss."); } };
  const edit = async (row) => { const nextAmount = window.prompt("Loss amount", String(row.amount)); if (nextAmount === null) return; const nextDescription = window.prompt("Description", row.description || ""); if (nextDescription === null) return; try { await updateLoss({ id: row.id, loss_type: row.loss_type, amount: Number(nextAmount), description: nextDescription }); const response = await listLosses(monthId, query, status); setRows(response?.losses || []); } catch (err) { setError(err.message || "Failed to edit loss."); } };
  const reverse = async (row) => { const reason = window.prompt("Reason for reversal", ""); if (reason === null) return; try { await reverseLoss({ id: row.id, reason }); const response = await listLosses(monthId, query, status); setRows(response?.losses || []); setNotice("Loss reversed and retained in history."); } catch (err) { setError(err.message || "Failed to reverse loss."); } };

  return <main className="opsPage"><div className="opsHeader"><div><p className="opsEyebrow">Operations / Losses</p><h1>Order-first losses</h1><p className="opsMuted">Search an existing order, choose one of its customers, and record the loss with an auditable relationship.</p></div><label>Month<select value={monthId} onChange={(event) => { setMonthId(event.target.value); setOrder(null); setCustomers([]); }}>{months.map((month) => <option key={month.id} value={month.id}>{month.name} (#{month.id})</option>)}</select></label></div>{error && <div className="opsAlert opsAlertError">{error}</div>}{notice && <div className="opsAlert opsAlertSuccess">{notice}</div>}<section className="opsCard"><div className="opsSectionTitle"><div><h2>Find an order</h2><p className="opsMuted">Only orders from the selected month and authenticated account are searchable.</p></div></div><input className="opsWideInput" value={orderQuery} onChange={(event) => setOrderQuery(event.target.value)} placeholder="Order name, order number, or details" /><div className="opsChoiceList">{orders.map((nextOrder) => <button className={order?.order_id === nextOrder.order_id ? "opsChoice opsChoiceActive" : "opsChoice"} key={nextOrder.order_id} onClick={() => chooseOrder(nextOrder)}><strong>{nextOrder.order_name || `Order #${nextOrder.order_id}`}</strong><span>#{nextOrder.order_id}</span></button>)}</div>{order && <div className="opsSelectedContext"><strong>Selected: {order.order_name || `Order #${order.order_id}`}</strong><select value={customerId} onChange={(event) => setCustomerId(event.target.value)}><option value="">Choose affected customer</option>{customers.map((customer) => <option key={customer.customer_id} value={customer.customer_id}>{customer.customer_name || "(empty)"} · cart {customer.cart_order_number || customer.cart_id}</option>)}</select><form className="opsInlineForm" onSubmit={save}><select value={lossType} onChange={(event) => setLossType(event.target.value)}><option>out of stock item</option><option>delivery charge</option><option>damaged item</option><option>other</option></select><input type="number" step="0.01" min="0.01" value={amount} onChange={(event) => setAmount(event.target.value)} placeholder="Amount" /><input value={description} onChange={(event) => setDescription(event.target.value)} placeholder="Description" /><button>Save loss</button></form></div>}</section><section className="opsCard"><div className="opsToolbar"><div><h2>Loss history</h2><p className="opsMuted">{rows.length} matching record(s)</p></div><div className="opsToolbarActions"><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search losses" /><select value={status} onChange={(event) => setStatus(event.target.value)}><option value="active">Active</option><option value="reversed">Reversed</option><option value="all">All</option></select></div></div>{!rows.length ? <div className="opsEmpty">No losses match this filter.</div> : <div className="opsTableWrap"><table className="opsTable"><thead><tr><th>Order</th><th>Customer</th><th>Type</th><th>Amount</th><th>Description</th><th>Status</th><th>Actions</th></tr></thead><tbody>{rows.map((row) => <tr key={row.id}><td>{row.order_name_snapshot || `#${row.order_id}`}<small>Cart {row.cart_order_number_snapshot || row.cart_id}</small></td><td>{row.customer_name_snapshot || `#${row.customer_id}`}</td><td>{row.loss_type}</td><td><strong>${money(row.amount)}</strong></td><td>{row.description || "-"}</td><td>{row.reversed_at ? `Reversed ${row.reversed_at}` : "Active"}</td><td>{!row.reversed_at && <><button onClick={() => edit(row)}>Edit</button><button onClick={() => reverse(row)}>Reverse</button></>}</td></tr>)}</tbody></table></div>}</section></main>;
}
